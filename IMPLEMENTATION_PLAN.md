# خطة التطبيق الشاملة - CIOB GMAO Enterprise v3.0.0
**المدة:** 12 أسبوع (60 يوم عملي) | **النموذج:** 100% Offline-First Clean Architecture Twin Excel Model

---

## 📅 المرحلة 1️⃣: بنية البيانات والـ Migrations (الأسابيع 1-2)

### الأسبوع 1: نظام النسخ وإصدارات المخطط (Schema & Migrations)
- **اليوم 1-2: Migrations Engine**
  - [x] إنشاء كلاس `MigrationRunner` لإدارة إصدارات قاعدة البيانات محلياً.
  - [x] توثيق إصدارات المخطط (`schema_versions`) وتتبع التحديثات التلقائية.
  - [x] دعم التراجع الآمن (`Rollback`) في حال حدوث أي خطأ أثناء الترقية.
- **اليوم 3-4: دعم العمليات الذرية (Transactions)**
  - [x] تفعيل `db.transaction(['spare_parts', 'movements'], 'readwrite', callback)`.
  - [x] ضمان التراجع التلقائي (`Abort/Rollback`) عند فشل أي خطوة في تسجيل حركة المخزون.
  - [x] توحيد تحديث رصيد المقال وتسجيل وصل الحركة وسجل التدقيق في عملية واحدة غير قابلة للتجزئة.
- **اليوم 5: قيود التكامل والمفاتيح الخارجية (Foreign Keys & Constraints)**
  - [x] فرض القيود المنطقية: منع الكميات السالبة في حركات الصرف (`quantite > 0`).
  - [x] التحقق من سلامة المرجع (`ref`) والنوع (`id_type`) والمنطقة (`id_zone`).
  - [x] منع حذف المقالات أو الآلات المرتبطة بحركات تاريخية مسجلة (`ON DELETE RESTRICT`).

### الأسبوع 2: الفهارس وسرعة الاستعلام (Indexing & Integrity)
- **اليوم 6-7: Indexes للأداء العالي**
  - [x] فهرسة المقالات بحسب الرمز المرجعي `ref` والنوع `id_type`.
  - [x] فهرسة الحركات بحسب التاريخ `date` والآلة `id_machine_registered` ونوع العملية `type`.
- **اليوم 8-10: اختبارات تكامل قاعدة البيانات**
  - [x] كتابة اختبارات Rollback عند حدوث استثناء.
  - [x] اختبار سرعة تنفيذ 10,000 عملية استعلام عبر الفهارس.

---

## 📅 المرحلة 2️⃣: معالجة الأخطاء والصلابة (الأسابيع 3-4)

### الأسبوع 3: نظام الاستثناءات الموحد (Custom Errors)
- **اليوم 11-12: شجرة الأخطاء المؤسسية (`ApplicationError`)**
  - [x] إنشاء `ApplicationError` كفئة أساسية مع طابع زمني وتفاصيل دقيقة.
  - [x] إنشاء `DatabaseError`, `ValidationError`, `NotFoundError`.
  - [x] إنشاء `PermissionError`, `ConflictError`, `InsufficientStockError`.
- **اليوم 13-15: تغطية الخدمات والمستودعات**
  - [x] تغليف عمليات المستودعات (`Repositories`) بـ `try-catch` موحد وموجه.
  - [x] تسجيل وتتبع الأخطاء في `LoggerService` بدون انهيار واجهة المستخدم.

### الأسبوع 4: حواجز الأخطاء والمراقبة (Error Boundary & Telemetry)
- **اليوم 16-18: React Error Boundary**
  - [x] توفير واجهة استرداد لطيفة ومرنة تتيح للمستخدم إعادة محاولة تحميل العرض أو التحديث.
  - [x] حفظ البيانات المدخلة في مسودة محلية قبل إعادة التحميل لحماية عمل المستخدم.
- **اليوم 19-20: اختبارات محاكاة الأعطال (Resilience Tests)**
  - [x] اختبار سلوك التطبيق عند تلف بيانات `localStorage` أو رفض أذونات التخزين.

---

## 📅 المرحلة 3️⃣: الأمان وإدارة الجلسات (الأسابيع 5-6)

### الأسبوع 5: إدارة الجلسات ومصادقة المستخدم (Session Management)
- **اليوم 21-23: Session Lifecycle**
  - [x] إنشاء `SessionService` لإنشاء رموز الجلسات (`Tokens`) وربطها بمعرف الجهاز `deviceId`.
  - [x] تطبيق مهلة عدم النشاط (`Inactivity Timeout`) بعد 30 دقيقة من الخمول لحماية المحطة.
  - [x] دعم تسجيل الخروج الفوري وإبطال الجلسة (`Session Revocation`).
- **اليوم 24-25: حماية الحسابات والتشفير**
  - [x] تشفير كلمات المرور ورموز الـ PIN عبر BCrypt و AES-256.
  - [x] عزل بيانات الرموز الحساسة في `sessionStorage` بدلاً من بقائها دائمة في التخزين المفتوح.

### الأسبوع 6: مصفوفة الصلاحيات الدقيقة (Fine-Grained RBAC)
- **اليوم 26-28: مصفوفة الصلاحيات (`AuthorizationService`)**
  - [x] تعريف مصفوفة `PERMISSIONS` لجميع الموارد: (Stock, Movements, Machines, Users, Reports, Settings).
  - [x] توفير دوال التحقق: `hasPermission`, `requirePermission`, `canAccess`.
- **اليوم 29-30: حماية الواجهات والأزرار**
  - [x] إخفاء أزرار الحذف والتعديل والإعدادات المتقدمة عن المستخدمين غير المخولين.
  - [x] حظر العمليات الحساسة برمجياً حتى في حال تلاعب العميل بواجهة المتصفح.

---

## 📅 المرحلة 4️⃣: دقة الحسابات والتوأم الرقمي لإكسيل (الأسابيع 7-8)

### الأسبوع 7: محرك الحسابات (`StockCalculationService`)
- **اليوم 31-33: مطابقة معادلات إكسيل (Excel Twin)**
  - [x] حساب الإدخالات: `calculateEntrees` مطابقة لـ `SUMIFS`.
  - [x] حساب الإخراجات: `calculateSorties` مطابقة لـ `SUMIFS`.
  - [x] حساب الرصيد الفعلي: `stockActuel = stockInitial + entrees - sorties`.
  - [x] تحديد حالات التنبيه: `RUPTURE` عند المخزون المنعدم أو السالب، و `ALERTE` عند بلوغ أو تدني الحد الأدنى.
- **اليوم 34-35: التحقق التلقائي مع ملفات الإكسيل المرجعية**
  - [x] إنشاء `verifyCalculations` للمقارنة الفورية مع أي شيت إكسيل مرجعي.
  - [x] إطلاق تحذيرات وإشعارات تدقيق عند اكتشاف أي تباين طفيف.

### الأسبوع 8: الإحصائيات الشاملة والتقارير (Analytics & Reports)
- **اليوم 36-38: المؤشرات المتقدمة (KPIs)**
  - [x] حساب القيمة الإجمالية للمخزون بالدرهم/اليورو (`totalValue`).
  - [x] نسب المقالات المتوفرة، والحرجة، وتلك التي في حالة نفاذ تام.
- **اليوم 39-40: الاختبارات الأحادية والتأكيدية (Unit Tests)**
  - [x] تغطية شاملة لجميع الحالات الحدية (Edge Cases) مثل المقالات بدون حركات أو الحركات ذات الكميات الكبيرة.

---

## 📅 المرحلة 5️⃣: الأداء والتحسين للبيانات الضخمة (الأسابيع 9-10)

### الأسبوع 9: التخزين المؤقت والصفحات (Caching & Pagination)
- **اليوم 41-43: التخزين المؤقت الذكي (`CacheService`)**
  - [x] دعم مدة الصلاحية `TTL` وإلغاء الصلاحية عبر الوسوم (`Tags Invalidation`).
  - [x] رصد وتتبع مؤشرات النجاح ومعدل الوصول للذاكرة المؤقتة (`hitRate`).
- **اليوم 44-45: التقسيم والصفحات (`PaginationService`)**
  - [x] تقسيم القوائم الضخمة إلى صفحات مرنة (50/100 سجل في الصفحة).
  - [x] حساب أرقام الصفحات وروابط التالي والسابق مع إمكانية القفز السريع.

### الأسبوع 10: المحاكاة الافتراضية للجداول (`useVirtualizer`)
- **اليوم 46-48: خطاف التقديم الافتراضي للجدول**
  - [x] إنشاء `useVirtualizer` لتقديم الصفوف المرئية فقط في مساحة العرض مع هوامش أمان (`overscan`).
  - [x] خفض استهلاك الذاكرة من 500MB إلى أقل من 20MB لسلاسة تمرير فائقة مع أكثر من 10,000 مقال.
- **اليوم 49-50: اختبارات الأداء الميدانية**
  - [x] اختبار سرعة استجابة التطبيق تحت ضغط 50,000 حركة مخزون.

---

## 📅 المرحلة 6️⃣: الاختبارات الشاملة والتوثيق والاعتماد (الأسابيع 11-12)

### الأسبوع 11: حزمة الاختبارات المؤتمتة (Automated Test Suite)
- **اليوم 51-53: Unit & Integration Tests (Vitest)**
  - [x] اختبارات `StockCalculationService.test.js`
  - [x] اختبارات `AuthorizationService.test.js`
  - [x] اختبارات `CacheService.test.js`
  - [x] اختبارات `PaginationService.test.js`
  - [x] اختبارات `SessionService.test.js`
- **اليوم 54-55: End-to-End & Offline Sync Tests**
  - [x] اختبارات دورة العمل الكاملة: إنشاء مقال -> تسجيل وصل خروج -> احتساب الرصيد الفعلي -> التحقق من حالة التنبيه.

### الأسبوع 12: التوثيق المؤسسي والجاهزية للإنتاج (Production Readiness)
- **اليوم 56-58: دليل المطور والهندسة المعمارية**
  - [x] توثيق نماذج الكيانات وتطابقها مع جداول إكسيل في `AGENTS.md` و `DOCS_INDEX.md`.
  - [x] توثيق استراتيجية النسخ الاحتياطي المشفر وتصدير/استيراد `.xlsx`.
- **اليوم 59-60: المراجعة النهائية والاعتماد**
  - [x] التحقق من نجاح جميع الاختبارات مع صفر أخطاء بناء أو Linting.
  - [x] الإطلاق الرسمي لإصدار **CIOB GMAO v3.0.0 Enterprise**.
